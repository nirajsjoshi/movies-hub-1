import styled from "styled-components";

export const Wrapper = styled.div`
    font-size: var(--fontBig);
    text-align: center;
    width: 100%;
    color: var(--white);
    position: absolute;
    transform: translate(0vw, -102vh);
`